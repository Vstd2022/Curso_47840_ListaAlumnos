import { ListaDirective } from './lista.directive';

describe('ListaDirective', () => {
  it('should create an instance', () => {
    const directive = new ListaDirective();
    expect(directive).toBeTruthy();
  });
});
